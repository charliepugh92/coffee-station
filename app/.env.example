# Base URL of the Rails API. Inlined into the bundle at build time; the GraphQL
# endpoint (/graphql) and the ActionCable WebSocket URL (http -> ws) are derived
# from it. Copy this file to `.env.local` to override locally.
VITE_API_URL=http://localhost:3000
