#import "../../fragments/OrderFields.graphql"

mutation CompleteOrder($orderId: ID!, $file: Upload!) {
  completeOrder(input: { orderId: $orderId, file: $file }) {
    order {
      ...OrderFields
    }
    errors
  }
}
