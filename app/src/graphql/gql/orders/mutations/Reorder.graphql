#import "../../fragments/OrderFields.graphql"

mutation Reorder($orderToken: String!) {
  reorder(input: { orderToken: $orderToken }) {
    order {
      ...OrderFields
    }
    guestToken
    errors
  }
}
