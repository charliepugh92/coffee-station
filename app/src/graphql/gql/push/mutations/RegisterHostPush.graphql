mutation RegisterHostPush($endpoint: String!, $p256dh: String!, $auth: String!) {
  registerHostPushSubscription(input: { endpoint: $endpoint, p256dh: $p256dh, auth: $auth }) {
    success
    errors
  }
}
