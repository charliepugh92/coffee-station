#import "../../fragments/OrderFields.graphql"

query StationBoard($id: ID!) {
  station(id: $id) {
    id
    name
    openSession {
      id
      status
      shareToken
      orders {
        ...OrderFields
      }
    }
  }
}
